services:
  app:
    build: .
    container_name: backend_app
    ports:
      - "3000:3000"
    env_file:
      - .env
    command: sh -c "npx prisma generate && npx prisma migrate deploy && node src/seed.js && npm start"
    restart: unless-stopped 
